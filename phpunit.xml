<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         cacheDirectory=".phpunit.cache"
         colors="true">
    <testsuites>
        <testsuite name="queue-ops">
            <directory>tests</directory>
        </testsuite>
    </testsuites>
    <php>
        <!-- Keep the suite deterministic and Redis-free: the framework's cache
             driver defaults to 'redis' (which fatals without the ext); array cache
             needs no service. Mirrors the framework's own phpunit.xml convention. -->
        <env name="CACHE_DRIVER" value="array"/>
    </php>
</phpunit>
